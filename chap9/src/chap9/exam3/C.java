package chap9.exam3;

import chap9.A;

public class C {
	//필드
	A a1 = new A(true); // (o)
	//A a2 = new A(1); //(x) default 다른 패키지 접근 불가
	//A a3 = new A("문자열"); //(x) private 다른 패키지 접근 불가
}
